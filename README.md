# Labs 10–13 (Spring Boot + JWT + SQLite)

REST API with:
- Register/Login (JWT access token)
- Refresh token rotation (HttpOnly cookie + DB)
- Logout (revocation)
- Flyway migrations
- Task CRUD with per-user access control

Run:
- set env JWT_SECRET (min 32 chars)
- start LabsApplication
